// const orm = require('../config/orm');

// const burgerAction = {
//   showAll: () => orm.selectAll(),

//   insert: (burger) => orm.insertOne(burger),

//   update: (id) => orm.updateOne(id)

// }

// module.exports = burgerAction;
